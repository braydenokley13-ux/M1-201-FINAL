# High-Quality Asset Slots

Drop optional GLB assets here for visual upgrade passes.

## Planned files
1. `arena_main.glb`
2. `mission_kiosk.glb`
3. `city_tower_set.glb`
4. `player_avatar.glb`

## Notes
- Keep textures compressed where possible.
- Use medium polycount for Chromebooks.
- Match scale in meters.

# TODO(ASSET-001): Replace placeholder scale values with per-model calibration after final art export.
